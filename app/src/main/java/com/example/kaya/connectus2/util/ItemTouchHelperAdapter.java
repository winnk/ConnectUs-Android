package com.example.kaya.connectus2.util;

/**
 * TODO: Add a class header comment!
 */

public interface ItemTouchHelperAdapter {
boolean onItemMove(int fromPosition, int toPosition);
void onItemDismiss(int position);
}
